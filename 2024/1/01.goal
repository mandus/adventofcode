#!/usr/bin/env goal
srt:{x@<x}
d:+"i"$!'=-read"input.txt"
p:srt'd
say "p1:", +/abs (*p)-(*|p)

l:*|d; r:*d / just store "left" and "right"
m:(?l)!=%l
say "p2:", +/(r)*(m[r]) / utilize that look-up in dict on unknowns yield 0
